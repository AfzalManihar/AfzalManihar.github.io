"use client";
import Image from "next/image";
import { motion } from "framer-motion";
import { FiArrowUpRight, FiGithub } from "react-icons/fi";
import type { Project } from "@/data/portfolio";
import { Badge } from "@/components/ui/badge";
export function ProjectCard({ project, featured = false }: { project: Project; featured?: boolean }) { const color = project.accent === "lime" ? "text-lime" : "text-cyan"; const edge = project.accent === "lime" ? "group-hover:border-lime/45" : "group-hover:border-cyan/45"; return <motion.article initial={{ opacity: 0, y: 22 }} whileInView={{ opacity: 1, y: 0 }} whileHover={{ y: -7 }} viewport={{ once: true, amount: .12 }} transition={{ duration: .55, ease: [0.22, 1, 0.36, 1] }} className={`group overflow-hidden rounded-2xl border border-white/[.09] bg-white/[.025] shadow-[0_20px_60px_rgba(0,0,0,.14)] transition-colors duration-500 ${edge} ${featured ? "lg:col-span-2" : ""}`}><div className="relative aspect-[16/8] overflow-hidden border-b border-white/[.08] bg-slate-950"><Image src={project.image} alt={`${project.title} interface preview`} fill className="object-cover transition duration-700 ease-out group-hover:scale-[1.07]" /><div className="absolute inset-0 bg-gradient-to-t from-ink/55 via-transparent to-transparent opacity-70" /><div className="absolute right-4 top-4 rounded-full border border-white/15 bg-ink/45 px-3 py-1.5 font-mono text-[10px] uppercase tracking-wider text-white opacity-0 backdrop-blur-md transition duration-300 group-hover:opacity-100">View case study</div></div><div className="p-6 sm:p-7"><p className={`font-mono text-[10px] font-medium uppercase tracking-[.18em] ${color}`}>{project.type}</p><h3 className="mt-3 font-display text-2xl font-bold tracking-tight text-white">{project.title}</h3><p className="mt-3 leading-7 text-slate-400">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.stack.map((item) => <Badge key={item}>{item}</Badge>)}</div><ul className="mt-6 space-y-2 text-sm text-slate-300">{project.features.map((feature) => <li key={feature} className="flex gap-2"><span className={color}>✦</span>{feature}</li>)}</ul><p className="mt-6 border-l-2 border-lime pl-3 text-sm font-medium leading-6 text-slate-200">{project.result}</p><div className="mt-7 flex gap-4 border-t border-white/[.08] pt-5"><a href={project.github} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm font-semibold text-white transition hover:text-cyan"><FiGithub /> Code</a><a href={project.demo} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm font-semibold text-white transition hover:text-cyan">Live demo <FiArrowUpRight className="transition group-hover:-translate-y-0.5 group-hover:translate-x-0.5" /></a></div></div></motion.article>; }
